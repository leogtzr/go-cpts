package main

import "fmt"

// the variable is beyond this function scope
// most likely, the compiler will allocate this variable in the heap.
func createPointer() *int {
	x := 23
	return &x
}

func main() {
	pX := createPointer()
	fmt.Println(*pX)

	*pX++

	fmt.Println(*pX)
}
